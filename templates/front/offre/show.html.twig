{% extends 'base.html.twig' %}

{% block title %}Offre{% endblock %}

{% block content %}
    <h1>Offre</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ offre.id }}</td>
        </tr>
        <tr>
            <th>Categ</th>
            <td>{{ offre.categ }}</td>
        </tr>
        <tr>
            <th>Nom</th>
            <td>{{ offre.nom }}</td>
        </tr>
        <tr>
            <th>Email</th>
            <td>{{ offre.email }}</td>
        </tr>
        <tr>
            <th>Logo</th>
            <td>{{ offre.logo }}</td>
        </tr>
        <tr>
            <th>Title</th>
            <td>{{ offre.title }}</td>
        </tr>
        <tr>
            <th>Description</th>
            <td>{{ offre.description }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('offre_list') }}">back to list</a>

    <a href="{{ path('offre_edit', {'id': offre.id}) }}">edit</a>

    {{ include('back/delte.html.twig') }}
{% endblock %}
