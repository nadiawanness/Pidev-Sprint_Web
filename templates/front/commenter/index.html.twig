{% extends 'base.html.twig' %}

{% block title %}Commenter index{% endblock %}

{% block body %}
    <h1>Commenter index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Commentaire</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for commenter in commenters %}
            <tr>
                <td>{{ commenter.id }}</td>
                <td>{{ commenter.commentaire }}</td>
                <td>
                    <a href="{{ path('commenter_show', {'id': commenter.id}) }}">show</a>
                    <a href="{{ path('commenter_edit', {'id': commenter.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('commenter_new') }}">Create new</a>
{% endblock %}
