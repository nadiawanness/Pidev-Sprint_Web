{% extends 'base.html.twig' %}

{% block title %}Reclamation{% endblock %}

{% block body %}
    <h1>Reclamation</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ reclamation.id }}</td>
        </tr>
        <tr>
            <th>Nom</th>
            <td>{{ reclamation.nom }}</td>
        </tr>
        <tr>
            <th>Description</th>
            <td>{{ reclamation.description }}</td>
        </tr>

        </tbody>
    </table>

    <a class="btn btn-secondary" href="{{ path('affiche_recla') }}">back to list</a>


{% endblock %}
