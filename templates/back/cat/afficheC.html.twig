{% extends 'base.html.twig' %}

{% block title %}Liste des evenement{% endblock %}

{% block content %}

<div class="main-panel">


   <h1>Liste Des Categories</h1>

    <form method="post" action="{{ path('recherchecatt') }}">
        <input type="text"  placeholder="Recherche" name ="recherche">
        <input type="submit" value="recherche"  class="btn btn-primary" >

    </form>


    <table border="2" class="table table-striped">

        <tr>
            <th>Id</th>
            <th>Nom</th>

            <th>Modifier</th>
            <th>Supprimer</th>

        </tr>

        <tr {% for c in cat %} >

            <td> {{ c.id }} </td>
            <td> {{ c.nom }} </td>

            <td ><a href="{{ path('modifiecatt',{'id':c.id}) }}"  class="badge badge-danger">Modifier</a></td>
            <td ><a href="{{ path('supprimecatt',{'id':c.id}) }}"  class="badge badge-danger">Supprimer</a></td>



        </tr {% endfor %} >
    </table>
    <a href="{{ path('ajoutcatt') }}"><input type="button" value="Ajouter une categorie" class="btn btn-danger"></a>
</div>


{% endblock %}
